#include <sstream>
#include <vector>
#include <iostream>
using namespace std;

void parseInts(string str) {
	for(int i=0;i<str.size();i++){
        if(str[i]==',')cout<<endl;
        else cout<<str[i];
    }
}

int main() {
    string str;
    cin >> str;
    parseInts(str);

    
    return 0;
}

